# How to use `canvaskit-wasm` in a Cloudflare Worker

This is a minimal example of how to use `canvaskit-wasm` in a Cloudflare Worker.

## Upload size

```
Total Upload: 7211.12 KiB / gzip: 2860.67 KiB
```

## CPU Usage

- The first run which includes WASM initialization took around 65 ms.
- After that, around 10-15ms.
